import { styled } from '@mui/material';

export const HeaderBlock = styled('header')(() => ({
    marginTop: 20,
}))

export const HeaderWrapper = styled('div')(() => ({
    backgroundColor: '#523737',
    maxWidth: 360,
    margin: '0 auto'
}))